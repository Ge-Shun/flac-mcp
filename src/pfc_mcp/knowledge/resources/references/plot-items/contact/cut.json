{
  "name": "cut",
  "parent": "contact",
  "description": "Cut plane for clipping contact visualization.",

  "status": "unverified",
  "notes": [
    "Cut plane syntax via command is unreliable — inline parameters may cause 'Unused extra parameter' errors.",
    "Recommended approach: configure cut planes interactively in PFC GUI, then export with 'plot export datafile filename \"<path>\"' to capture the working syntax."
  ]
}
